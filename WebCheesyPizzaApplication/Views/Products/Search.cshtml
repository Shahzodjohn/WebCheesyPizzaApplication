@model List<WebCheesyPizzaApplication.Models.Product>

@{
    ViewData["title"] = "Поиск по блюдам";
}


<div class="row">
    <div class="row">
        @foreach (var item in Model)
        {
            <div class="col-3 mt-3">
                <div asp-validation-summary="All"></div>
                <div class="card">
                    <div class="card-header  text-center" style="height:70px">
                        <a href="~/products/detailsproduct?id=@item.Id" class="btn btn-link"><strong>@item.Name</strong></a>
                    </div>
                    <div class="card-body d-flex justify-content-center">
                        <img width="320" src="@item.Image" alt="PHOTO" />
                    </div>
                    <div class="card-footer  d-flex justify-content-between">
                        <p><strong>Цена: @item.Price TJS </strong> </p>
                        <a href="~/products/AddToBasket?id=@item.Id">
                            <svg xmlns="http://www.w3.org/2000/svg" width="36" height="36" fill="currentColor" class="bi bi-bag-plus" viewBox="0 0 16 16">
                                <path fill-rule="evenodd" d="M8 7.5a.5.5 0 0 1 .5.5v1.5H10a.5.5 0 0 1 0 1H8.5V12a.5.5 0 0 1-1 0v-1.5H6a.5.5 0 0 1 0-1h1.5V8a.5.5 0 0 1 .5-.5z" />
                                <path d="M8 1a2.5 2.5 0 0 1 2.5 2.5V4h-5v-.5A2.5 2.5 0 0 1 8 1zm3.5 3v-.5a3.5 3.5 0 1 0-7 0V4H1v10a2 2 0 0 0 2 2h10a2 2 0 0 0 2-2V4h-3.5zM2 5h12v9a1 1 0 0 1-1 1H3a1 1 0 0 1-1-1V5z" />
                            </svg>
                        </a>
                    </div>
                </div>
            </div>

        }

    </div>
</div>




    @*<div class="row">
        <div class="col-md-3">
            <div asp-validation-summary="All"></div>
            <div class="row">
                <form asp-action="DetailsProduct" asp-controller="Products">
                    <div class="card">
                        <div class="card-header">
                            <div class="text-center">
                                <a href="~/products/detailsproduct?id=@item.Id">@item.Name</a>
                            </div>
                        </div>
                        <div class="card-body">
                            <div type="text" asp-for="Id"></div>
                            <div>
                                <img width="320" src="@item.Image" alt="PHOTO" />
                            </div>
                        </div>
                        <div class="card-footer">
                            <div>Цена: @item.Price</div>
                            <div>Категория: @item.Category.Name</div>
                            <a href="~/products/AddToBasket?id=@item.Id" style="margin-top:10px" class=" btn sbtn-block btn-primary btn-outline-success btn-block">Добавить в корзину</a>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>*@





